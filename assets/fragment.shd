#version 450

in vec4 objectColor;

uniform sampler2D texture_sampler;

out vec4 outputColor;

void main()
{
    //fragColor = texture(texture_sampler, gl_PointCoord);
    outputColor = objectColor;
}
